[package]
name = "pkpasser_cli"
description = "Simple command line utility to create, sign and bundle .pkpass files"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
pkpasser_core = { path = "../pkpasser_core" }
